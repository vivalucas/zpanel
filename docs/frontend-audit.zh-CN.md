# React 重构自查（2026-09-13）

本次对照 Git 中重构前实际可访问的页面和接口，审查当前 React 实现，并修复发现的问题。下表区分已实现、自动化验证和仍需实机验收的内容；不能把编译通过等同于全部集成已经验收。

## 功能与入口对照

| 功能 | 当前入口与实现 | 验证范围 |
|---|---|---|
| 登录与验证码 | `/#/login`，失败刷新验证码，保留输入 | 真实登录；模拟验证码失败；缺少 `randomUUID` 的 HTTP 能力环境 |
| 导航项目 | 首页添加、更多菜单编辑/删除；文字、图片、Iconify 图标；分组选择、内外网地址、三种打开方式 | 添加、编辑、搜索、键盘排序与三种打开方式/内外网选择连接真实 API；安全 URL 规则有回归；外部网站是否允许 iframe 取决于对方 |
| 分组 | 设置 → 导航分组，添加、编辑、删除、拖拽/键盘排序 | 真实分组创建；删除及排序接口原有 Go 回归；服务端保留至少一个分组 |
| 外观与主题 | 设置 → 风格设置；统一 Ant Design token、深浅色、跟随系统 | 真实保存、失败保留、保存锁定、离开提醒、深色控件实际颜色 |
| 搜索引擎 | 设置 → 搜索与监控；首页选择可保存默认引擎 | 真实保存；删除前置引擎不会改变选中的默认项 |
| 系统监控 | 设置 → 搜索与监控；CPU、内存、磁盘及颜色/路径 | 真实 CPU/内存展示、模块保存；磁盘依赖宿主机可见挂载点 |
| 图片 | 设置 → 文件图库；上传、预览、壁纸、引用查询/替换/删除 | 真实上传、壁纸及引用；模拟多页列表、跨页替换和末页删除 |
| 配置导入导出 | 设置 → 导入与导出；追加/覆盖、选择内容、摘要与幂等重试 ID | 真实导出/追加导入；原有校验、失败中止、排序和后端事务/幂等回归 |
| 个人账号 | 设置 → 个人账号；资料、语言、账户切换、密码、退出 | 多账户隔离、密码修改与旧会话撤销；语言资源切换 |
| 用户与公开访问 | 管理员设置 → 账号管理 | 创建/编辑/删除、公开只读访问；普通用户后台入口 403 |
| Docker | 管理员设置 → Docker；列表、状态、资源、操作确认、日志 | 真实不可连接错误；成功操作、失败重试和日志使用模拟 API，不代表真实容器验收 |
| 站点与关于 | 管理员站点设置；所有登录用户可打开关于 | 标题、脚本、安全模式；版本与说明页面 |
| PWA | 可选构建开关，默认关闭 | 独立构建、Go 静态路由；不含系统安装/升级验收 |

原来的个人信息、风格、分组、上传、导入导出、关于、用户、Docker 弹窗均有页面入口。站点配置从外观中独立出来，搜索与监控配置集中到搜索与监控。普通用户不显示管理入口；手机选择器与桌面侧栏使用同一份菜单定义。

## 本次发现并修复

- 手工拆分第三方依赖在导入变化后出现循环依赖，构建成功但浏览器白屏；移除不稳定的手工分包，使用 Vite 默认共享依赖拆分，保留页面延迟加载并通过生产浏览器回归。
- HTTP 局域网缺少 `crypto.randomUUID` 时登录页崩溃；统一改用可在 HTTP 使用的随机 ID。
- 旧请求或确认回调在切换账户后继续影响当前会话；请求绑定会话并丢弃过期响应，个人资料保存不再切回旧账户。
- 默认引擎以索引存储，删除它之前的引擎时会错位；现在同步调整索引，并恢复首页默认引擎持久化与图标展示。
- 保存期间编辑及外观回填可能覆盖输入；保存期间禁用表单，成功刷新后才解除脏标记，失败保留输入。外观、组件、站点设置增加未保存离开提醒。
- 项目编辑框会随分组查询刷新而重新初始化；改为打开编辑框时初始化一次。
- 文件末页删除后分页入口消失，引用替换只能选择当前页；保留分页入口并增加独立的替换来源与分页选择。
- 自定义脚本随整个站点对象变化重复执行，hash 安全模式不能即时响应；拆分标题、CSS、JS 更新，并对脚本恢复执行整页重载。
- 全局加载图标参与按钮无障碍名称，快速失败后的按钮可能残留 `loading` 名称；通过 Ant Design 公开配置提供装饰性加载图标，保持按钮名称稳定。
- 不存在的设置路径误报 403；现在按实际权限或不存在分别显示 403/404。
- 可选 PWA 构建生成的根路径资源未被 Go 提供；增加明确资源路由，避免开放任意工作目录文件。
- 贡献指南与更新日志遗漏重构和测试说明；同步 README、前端说明、贡献指南、更新日志与本自查记录。

## 验证方法与边界

本轮最终结果：15 项 Playwright 浏览器测试全部通过，9 项 Vitest 单元测试全部通过；`test:review`、TypeScript、ESLint、`go test ./...`、冻结锁文件安装与普通/PWA 构建通过。普通构建保留下面说明的共享脚本体积警告。`git diff --check`、文档本地链接和静态中英文文案键检查通过。

质量检查命令见 [贡献指南](../CONTRIBUTING.md)。浏览器测试使用生产构建及独立 Go 服务、临时 SQLite 数据库，端口 16521；不会访问开发数据库，Docker 上下文被隔离。API 与导航/备份回归、TypeScript、ESLint、生产构建及 `go test ./...` 均属于验收检查。

UI 使用同一 Ant Design 组件库、主题 token、共享设置卡片/错误提示/加载状态。浏览器回归检查桌面和 360/390px 手机宽度、深色控件颜色和页面水平溢出；截图用于人工复核。当前验收浏览器为 Chromium，不代表 Safari、Firefox 和所有触屏设备兼容性已验收。

仍需实机验收：真实 Docker 容器操作、Ubuntu Docker 构建部署与完整恢复、MySQL/Redis、跨设备真实磁盘指标、PWA 安装和升级。验证码失败流程通过模拟业务响应验证，成功验证码识别未做自动化。

新增文案完整提供简体中文和英文，其余语言复用旧资源并回退英文，尚未完成全部新增文案翻译。配置 JSON 只包含导航与外观，不包括模块配置、上传文件、用户和密码；完整备份仍需备份 `conf` 与 `data`。不存在“配置导出等于整站备份”的承诺。

当前共享入口脚本约 1.02 MB（gzip 约 328 KB），构建保留体积警告。为避免依赖循环，不通过强制拆小包或提高警告阈值掩盖它；页面已延迟加载，进一步压缩首屏依赖仍是性能优化项。
