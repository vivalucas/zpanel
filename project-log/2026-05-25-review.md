# 2026-05-25 复查修复记录

**日期**：2026-05-25  
**范围**：针对 2026-05-24 全量评审中已确认要修的项做定向修复与复查  
**方法**：逐条回看修改前后代码，确认行为变化是否与评审结论一致

---

## 本轮修复

| 编号 | 原问题 | 本轮修改 | 结果 |
|------|--------|----------|------|
| C-2 | `GetDiskStateByPath` 被误判为可任意探测路径 | 仅允许已知挂载点，先从 `GetDiskMountpoints()` 校验 `req.Path`，不在列表内直接拒绝 | 公开模式下的路径探测面收窄，问题关闭 |
| H-5 | `highlightBlock` 直接拼接 `lang` | 新增 `sanitizeHighlightLang()`，只保留安全字符集后再拼接到 HTML / class | 降为防御性硬化项，当前无直接注入面 |
| C-3 | Docker 最终镜像以 root 运行 | 最终镜像新增 `zpanel` 用户，`USER zpanel` 运行，去掉 runtime `bash` / `su-exec` | 非 root 运行生效 |
| L-3 | 最终镜像多余 `bash` | runtime 层不再安装 `bash`，并将最终基础镜像固定为 `alpine:3.22` | 描述已修正，同时顺手消掉了运行时冗余包 |

---

## 代码落点

- [service/api/api_v1/system/monitor.go](</Users/lucas/projects/zpanel/service/api/api_v1/system/monitor.go:62>)：磁盘状态接口改为先校验挂载点。
- [src/views/home/components/Result/index.vue](</Users/lucas/projects/zpanel/src/views/home/components/Result/index.vue:21>)：高亮语言名做白名单过滤。
- [Dockerfile](</Users/lucas/projects/zpanel/Dockerfile:1>)：runtime 镜像改为非 root 用户，且固定 Alpine 版本。

---

## 复查结论

1. 这次修改没有引入新的编译错误。
2. `go test ./...` 通过。
3. `corepack pnpm type-check` 通过。
4. 本地没有 `docker` 命令，无法在当前环境里直接复跑镜像构建。

---

## 仍保留的后续项

- `C-1` SSRF 防护
- `H-1` 请求体大小限制
- `H-2` `Password` 序列化风险
- `H-4` URL 协议白名单
- `H-6` `footerHtml` 渲染策略
- `C-4` `build.sh` 下载链校验

这些项仍属于后续单独处理范围，本轮没有继续扩展。
